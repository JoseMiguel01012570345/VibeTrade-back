# Copy to .env and set POSTGRES_PASSWORD to a long random value.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=vibetrade
POSTGRES_USER=vibetrade
POSTGRES_PASSWORD=

# Host port mapped to the API container (docker compose). Default: 5110
# API_HTTP_PORT=5110
